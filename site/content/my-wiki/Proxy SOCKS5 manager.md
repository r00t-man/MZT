# 🚀 SOCKS5 (Dante) Manager for Telegram

Удобный Bash-скрипт для быстрой установки и управления **SOCKS5 прокси на Dante** под **Ubuntu** / **Debian**.

Скрипт умеет:

- ⚡ устанавливать и переустанавливать **Dante**
- 👤 создавать основного пользователя `gisproxy`
- 🔐 генерировать случайные пароли в формате `hex`
- ➕ добавлять новых пользователей
- 🗑️ удалять пользователей
- ♻️ менять пароль пользователю
- 🔎 показывать текущие настройки прокси
- 🔗 генерировать готовые **Telegram-ссылки**
- ✏️ открывать конфиг на редактирование
- 🌐 менять порт прокси
- 🛡️ предлагать открыть порт в **UFW**

---

# 📦 Установка в один клик

Самый простой способ — сразу скачать и запустить скрипт одной командой:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/r00t-man/MZT/refs/heads/main/files/Proxy_socks5_dante.sh)
````

> Запускать от `root` или через `sudo -i`

---

# 📥 Установка через скачивание файла

Если хочешь сначала скачать скрипт, а потом запускать вручную:

```bash
wget -O Proxy_socks5_dante.sh https://raw.githubusercontent.com/r00t-man/MZT/refs/heads/main/files/Proxy_socks5_dante.sh
chmod +x Proxy_socks5_dante.sh
sudo ./Proxy_socks5_dante.sh
```

---

# 🖥️ Что делает скрипт

После запуска откроется меню управления:

```text
1)  Установить / переустановить SOCKS5 (Dante)
2)  Показать текущие настройки прокси
3)  Добавить нового пользователя
4)  Удалить пользователя
5)  Сменить пароль пользователю
6)  Показать ссылку для выбранного пользователя
7)  Удалить всех пользователей кроме gisproxy
8)  Редактировать /etc/danted.conf
9)  Сменить порт прокси
10) Перезапустить danted
11) Показать статус danted
0)  Выход
```

---

# ⚙️ Первая установка

После запуска скрипта выбери:

```text
1) Установить / переустановить SOCKS5 (Dante)
```

Далее скрипт предложит выбрать порт:

* `8443` — ✅ рекомендуется
* `9443` — ✅ рекомендуется
* `1194` — если нужен альтернативный порт
* `51820` — если нужен альтернативный порт
* свой порт

После установки скрипт покажет:

* 🌐 IP сервера
* 🔌 порт
* 👤 логин
* 🔐 пароль
* 🔗 готовую ссылку для Telegram

Пример вывода:

```text
=== SOCKS5 (Dante) ready ===
Iface   : ens3
IPv4    : 203.0.113.36
Port    : 8443
User    : gisproxy
Pass    : a3f7c9e2b6d1f048e5a9c2b7d4f18e63
Listen  : yes
```

---

# 🔗 Подключение в Telegram

Скрипт автоматически генерирует ссылку вида:

```text
https://t.me/socks?server=203.0.113.36&port=8443&user=gisproxy&pass=PASSWORD
```

### Как подключить:

1. Скопируй ссылку
2. Отправь её себе в Telegram
3. Нажми на неё
4. Telegram сам предложит добавить SOCKS5-прокси

---

# 👤 Добавление нового пользователя

Если нужно выдать прокси другому человеку:

```text
3) Добавить нового пользователя
```

Скрипт:

* создаст нового пользователя Linux
* сгенерирует ему случайный пароль
* покажет готовую ссылку Telegram

---

# 🔐 Смена пароля пользователю

Если нужно перевыпустить доступ:

```text
5) Сменить пароль пользователю
```

Скрипт:

* сгенерирует новый случайный пароль
* обновит его у пользователя
* покажет новую ссылку подключения

---

# 🗑️ Удаление пользователя

Для удаления одного пользователя:

```text
4) Удалить пользователя
```

Для удаления всех, кроме основного `gisproxy`:

```text
7) Удалить всех пользователей кроме gisproxy
```

---

# 🌐 Смена порта прокси

Если нужно перенести SOCKS5 на другой порт:

```text
9) Сменить порт прокси
```

Можно выбрать один из рекомендуемых портов или указать свой вручную.

---

# ✏️ Ручное редактирование конфига

Если нужно изменить конфиг вручную:

```text
8) Редактировать /etc/danted.conf
```

Основной конфиг находится здесь:

```bash
/etc/danted.conf
```

После ручного редактирования рекомендуется выполнить:

```text
10) Перезапустить danted
```

---

# 📊 Просмотр текущих настроек

Для просмотра всех текущих параметров:

```text
2) Показать текущие настройки прокси
```

Показываются:

* статус сервиса
* сетевой интерфейс
* IP
* порт
* список пользователей
* пароли
* Telegram-ссылки

---

# 🧾 Где хранятся данные

### Конфиг Dante

```bash
/etc/danted.conf
```

### Резервная копия конфига

```bash
/etc/danted.conf_bak
```

### Локальное хранилище пользователей и паролей

```bash
/root/.dante_socks_users
```

> В этом файле отображаются только пользователи, которые были добавлены именно этим скриптом.

---

# 🛡️ Firewall / UFW

Если на сервере активен **UFW**, скрипт предложит автоматически открыть выбранный порт для:

* `TCP`
* `UDP`

Если ты отказался при установке, открыть порт можно вручную:

```bash
ufw allow 8443/tcp
ufw allow 8443/udp
```

Заменяй `8443` на свой выбранный порт.

---

# ✅ Проверка работы

Проверить статус сервиса:

```bash
systemctl status danted --no-pager -l
```

Проверить, слушает ли порт:

```bash
ss -tulpn | grep danted
```

Посмотреть логи:

```bash
journalctl -u danted -n 100 --no-pager
```

---

# 🧰 Требования

* Ubuntu / Debian
* root-доступ
* установленный `curl` или `wget`

---

# 💡 Рекомендации по портам

Обычно лучше использовать:

* `8443`
* `9443`

Альтернативы:

* `1194`
* `51820`

Если какой-то порт уже занят, выбери другой.

Проверить занятость порта можно так:

```bash
ss -tulpn | grep 8443
```

---

# 📌 Примечания

* основной пользователь по умолчанию: `gisproxy`
* пароли генерируются автоматически
* SOCKS5 в Telegram подключается через готовую ссылку
* скрипт рассчитан на удобное управление несколькими пользователями

---

# 🔗 Ссылка на скрипт

Raw-файл скрипта:

```text
https://raw.githubusercontent.com/r00t-man/MZT/refs/heads/main/files/Proxy_socks5_dante.sh
```

---

# ⭐ Быстрый старт

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/r00t-man/MZT/refs/heads/main/files/Proxy_socks5_dante.sh)
```

---
